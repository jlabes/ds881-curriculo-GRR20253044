{
  "name": "curriculo-johnny",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite src --host 0.0.0.0 --port 8080",
    "build": "vite build src --base=/ds881-curriculo-GRR20253044/ --outDir ../dist && mkdir -p ../dist/css ../dist/js ../dist/data && cp src/css/* ../dist/css/ 2>/dev/null || true && cp src/js/* ../dist/js/ 2>/dev/null || true && cp src/data/* ../dist/data/ 2>/dev/null || true"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}